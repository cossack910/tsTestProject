version: "3"

services:
  tstestproject:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/tsTestProject
    tty: true
    command: npm start
    ports:
      - 5555:80
    container_name: tstestproject
