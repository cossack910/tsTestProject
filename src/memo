memo

2023/7/5
event.preventDefault();

フォームの送信イベント (submit) で、フォームが自動的に送信されるのを防止してバリデーションを行う。
ハイパーリンク (<a> 要素) のクリックイベント (click) で、ページ遷移をキャンセルして別の操作を行う。
マウスボタン (mousedown, mouseup, click) のデフォルトの挙動を無効化して、特定のマウス操作のみを処理する。
preventDefault メソッドは、イベントオブジェクトに対して呼び出されるため、イベントリスナーの引数にイベントオブジェクトが渡されることが前提。
